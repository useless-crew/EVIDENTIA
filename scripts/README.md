# Repo-Level Scripts

Placeholder for repository-wide helper scripts (e.g. cross-service
orchestration, CI helpers).

Backend-specific scripts live in `backend/scripts/`.

TODO: Add scripts as needed once implementation begins.
