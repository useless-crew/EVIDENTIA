// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: auth_sessions.sql

package generated

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createAuthSession = `-- name: CreateAuthSession :one

INSERT INTO auth_sessions (user_id, family_id, token_hash, expires_at, ip_address, user_agent)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, user_id, family_id, token_hash, expires_at, created_at, last_used_at, revoked_at, replaced_by, ip_address, user_agent
`

type CreateAuthSessionParams struct {
	UserID    uuid.UUID `json:"user_id"`
	FamilyID  uuid.UUID `json:"family_id"`
	TokenHash []byte    `json:"token_hash"`
	ExpiresAt time.Time `json:"expires_at"`
	IpAddress *string   `json:"ip_address"`
	UserAgent *string   `json:"user_agent"`
}

// Evidentia — Auth Session (Refresh Token) Queries
//
// token_hash is always SHA-256(raw refresh token) — the raw token itself
// is never a column here and never passed to these queries. See
// internal/auth/refresh.go and internal/service/auth_service.go.
func (q *Queries) CreateAuthSession(ctx context.Context, arg CreateAuthSessionParams) (AuthSession, error) {
	row := q.db.QueryRow(ctx, createAuthSession,
		arg.UserID,
		arg.FamilyID,
		arg.TokenHash,
		arg.ExpiresAt,
		arg.IpAddress,
		arg.UserAgent,
	)
	var i AuthSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.FamilyID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.RevokedAt,
		&i.ReplacedBy,
		&i.IpAddress,
		&i.UserAgent,
	)
	return i, err
}

const getAuthSessionByTokenHash = `-- name: GetAuthSessionByTokenHash :one
SELECT id, user_id, family_id, token_hash, expires_at, created_at, last_used_at, revoked_at, replaced_by, ip_address, user_agent
FROM auth_sessions
WHERE token_hash = $1
`

func (q *Queries) GetAuthSessionByTokenHash(ctx context.Context, tokenHash []byte) (AuthSession, error) {
	row := q.db.QueryRow(ctx, getAuthSessionByTokenHash, tokenHash)
	var i AuthSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.FamilyID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.RevokedAt,
		&i.ReplacedBy,
		&i.IpAddress,
		&i.UserAgent,
	)
	return i, err
}

const revokeAllAuthSessionsForUser = `-- name: RevokeAllAuthSessionsForUser :exec
UPDATE auth_sessions
SET revoked_at = now()
WHERE user_id = $1 AND revoked_at IS NULL
`

// Invalidates every still-active session belonging to a user, regardless
// of family — used by admin user management (password reset,
// deactivation/suspension) so an already-issued refresh token cannot
// keep a session alive past that action.
func (q *Queries) RevokeAllAuthSessionsForUser(ctx context.Context, userID uuid.UUID) error {
	_, err := q.db.Exec(ctx, revokeAllAuthSessionsForUser, userID)
	return err
}

const revokeAuthSession = `-- name: RevokeAuthSession :exec
UPDATE auth_sessions
SET revoked_at = now()
WHERE id = $1 AND revoked_at IS NULL
`

// Plain revocation with no replacement — used by logout.
func (q *Queries) RevokeAuthSession(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, revokeAuthSession, id)
	return err
}

const revokeAuthSessionAndReplace = `-- name: RevokeAuthSessionAndReplace :exec
UPDATE auth_sessions
SET revoked_at = now(), last_used_at = now(), replaced_by = $2
WHERE id = $1 AND revoked_at IS NULL
`

type RevokeAuthSessionAndReplaceParams struct {
	ID         uuid.UUID  `json:"id"`
	ReplacedBy *uuid.UUID `json:"replaced_by"`
}

// Marks a session used and revoked in the same step, recording which new
// session replaced it — the "rotation" half of refresh-token rotation.
func (q *Queries) RevokeAuthSessionAndReplace(ctx context.Context, arg RevokeAuthSessionAndReplaceParams) error {
	_, err := q.db.Exec(ctx, revokeAuthSessionAndReplace, arg.ID, arg.ReplacedBy)
	return err
}

const revokeAuthSessionFamily = `-- name: RevokeAuthSessionFamily :exec
UPDATE auth_sessions
SET revoked_at = now()
WHERE family_id = $1 AND revoked_at IS NULL
`

// Invalidates every still-active session descending from the same login
// as sessionID's family — used when a revoked/rotated token is presented
// again (reuse detection: see master prompt §25).
func (q *Queries) RevokeAuthSessionFamily(ctx context.Context, familyID uuid.UUID) error {
	_, err := q.db.Exec(ctx, revokeAuthSessionFamily, familyID)
	return err
}
